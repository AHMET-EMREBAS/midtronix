import { IUserPhone } from '@mdtx/common';

export const USER_PHONE_COLUMNS: (keyof IUserPhone)[] = ['id', 'phone'];
export const USER_PHONE_DISPLAY_COLUMNS: (keyof IUserPhone)[] = ['id', 'phone'];
export const USER_PHONE_PAGE_SIZE = 500;
