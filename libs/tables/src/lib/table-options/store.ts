import { IStore } from '@mdtx/common';

export const STORE_COLUMNS: (keyof IStore)[] = ['id', 'name'];
export const STORE_DISPLAY_COLUMNS: (keyof IStore)[] = ['id', 'name'];
export const STORE_PAGE_SIZE = 500;
