import { ISprint } from '@mdtx/common';

export const SPRINT_COLUMNS: (keyof ISprint)[] = ['id', 'name'];
export const SPRINT_DISPLAY_COLUMNS: (keyof ISprint)[] = ['id', 'name'];
export const SPRINT_PAGE_SIZE = 500;
