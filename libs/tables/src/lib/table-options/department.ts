import { IDepartment } from '@mdtx/common';

export const DEPARTMENT_COLUMNS: (keyof IDepartment)[] = ['id', 'name'];
export const DEPARTMENT_DISPLAY_COLUMNS: (keyof IDepartment)[] = ['id', 'name'];
export const DEPARTMENT_PAGE_SIZE = 500;
