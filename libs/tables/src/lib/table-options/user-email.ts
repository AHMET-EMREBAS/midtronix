import { IUserEmail } from '@mdtx/common';

export const USER_EMAIL_COLUMNS: (keyof IUserEmail)[] = ['id', 'email'];
export const USER_EMAIL_DISPLAY_COLUMNS: (keyof IUserEmail)[] = ['id', 'email'];
export const USER_EMAIL_PAGE_SIZE = 500;
