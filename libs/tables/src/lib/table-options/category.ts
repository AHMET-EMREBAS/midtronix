import { ICategory } from '@mdtx/common';

export const CATEGORY_COLUMNS: (keyof ICategory)[] = ['id', 'name'];
export const CATEGORY_DISPLAY_COLUMNS: (keyof ICategory)[] = ['id', 'name'];
export const CATEGORY_PAGE_SIZE = 500;
