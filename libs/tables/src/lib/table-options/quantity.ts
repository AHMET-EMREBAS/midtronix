import { IQuantity } from '@mdtx/common';

export const QUANTITY_COLUMNS: (keyof IQuantity)[] = ['id', 'quantity'];
export const QUANTITY_DISPLAY_COLUMNS: (keyof IQuantity)[] = ['id', 'quantity'];
export const QUANTITY_PAGE_SIZE = 4;
